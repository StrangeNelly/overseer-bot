# Groupie — project notes for Claude

## What this is

"Groupie" (working title) is a web dashboard companion for a private crypto-trading Telegram group. The group posts token contract addresses ("calls") and pre-launch X accounts all day; in-group bots (Rick, Phanes) already track call market caps and multiples inside Telegram. Groupie lives OUTSIDE Telegram: a board members check to catch up on what was called, what's pumping, what died, and what's about to launch.

## Product principles (from the owner)

- Simple. No heavy filtering UI — the group chat IS the filter/curation layer.
- Read-only dashboard; interaction happens via Telegram commands (e.g. `/groupie addlaunchmonitor @project51`).
- Time windows: 6/12/24 hours, 3 days, week, month.
- Each token card: main coin details + deep links to trading apps (Axiom and similar).
- "Died after call" section; group chooses to keep in memory or remove.
- "Retraced" board: coins that pumped hard after the call (banger, e.g. 3x+) and have pulled back well off their peak but are NOT dying (liquidity intact, volume flowing). Same machinery as died-detection, different thresholds. Present as neutral data (retrace %, LP, volume) — never "buy opportunity" labels.
- The bot stays near-silent in Telegram: no recurring digest/summary posts (Rick/Phanes already do summaries; the chat is noisy enough). At most: reply to /groupie with the board link + a pinned message. The app is the calm, visual surface; the chat is the input.
- Track peak-since-call ourselves by polling from the moment a CA is posted — call-relative data (call MC, ATH-since-call, retrace %) is Groupie's moat and avoids paying for historical OHLCV APIs.
- v1 targets Robinhood Chain (#HOOD, EVM addresses) only. Multi-chain later.
- Built for one group first; designed so any group can add it later (multi-group SaaS potential).
- Visual polish comes after structure ("Claude design will eventually need to have a look at it").

## Working style (owner preference, 2026-09-01)

- Implementation is done by **Opus 5 subagents** (Workflow/Agent with `model: 'opus'`), driven by precise specs (file paths, interfaces, product rules from these docs). The main session designs, integrates, and adversarially reviews. Small glue edits/fixes stay inline.

## Where things live

- `docs/` — research reports, decisions, and planning documents. Read these before making architecture choices; they are the project's memory.

## Status log

- 2026-09-01: Project started. API/architecture research workflow completed; findings in `docs/research-*.md`, synthesis in `docs/research-summary.md`.
- 2026-09-01: v1 decisions locked in `docs/decisions.md` — TS monorepo, grammY bot (long polling), Hono + Vite/React Mini App, Supabase Postgres + Drizzle, Node process on Railway, GeckoTerminal-primary market data, clean (non-referral) trading links, X monitor deferred to v1.5, owner-supplied death thresholds. Next: `docs/plan.md` build plan, then scaffold.
- 2026-09-01: Round-2 decisions (Mini App confirmed; links = Axiom/GMGN/DexScreener only; any member can bin; repost semantics defined) and `docs/plan.md` written (M0 scaffold → M1 bot ingest → M2 data engine → M3 Mini App board → M4 deploy/beta → M5 v1.5). Ready to scaffold on owner go-ahead.
- 2026-09-01: M0+M1 built (npm-workspaces monorepo: packages/shared, packages/db, apps/server) and hardened via 19-agent adversarial review (13 confirmed findings fixed — see commit 1f645a2).
- 2026-09-01: M1 verified LIVE. Bot = @overseergroupbot (owner-created; privacy disabled, admin in test group). Supabase project npyjgnuskcexskxydehr (ap-northeast-2, fresh project, migration 0000 applied; MCP server configured in .mcp.json). Call + repost + /groupie all proven against the real group: caller credit preserved, mentions_count bumps, clock never resets. Dev-box gotcha: unroutable IPv6 blackholes Node fetch — index.ts forces ipv4first + disables autoSelectFamily; keep that in mind for deploys. NEXT: M2 market-data engine (GeckoTerminal primary, DexScreener refresher, mcap-at-call, peak tracking, death/retrace rules per decisions.md).
- 2026-09-01: M2 complete (commit 3de0349): market-data engine live-verified. Opus agent applied 18 review findings; Fable review added the revival migrated-pool fix. Migration 0001 applied. NEXT: M3 Mini App board (auth, board API, SSE, React UI) — spec for Opus agents.
- 2026-09-01: M3 complete (commit c897f23): Mini App board live in browser with real data. Two parallel Opus agents (backend + frontend) against packages/shared/src/api.ts contract; 9 review findings fixed incl. CSRF (hono/csrf Origin allowlist on mutating routes — cookie is SameSite=None for the TG webview), fail-closed dev-auth (ENABLE_DEV_AUTH opt-in), per-call died_at/death_reason (migration 0002 applied). Board sections: fresh/runners/retraced/died. Dev browsing: ENABLE_DEV_AUTH=true + DEV_AUTH_USER_ID in .env, open /g/<slug>. NEXT: M4 — register Mini App in BotFather, deploy to Railway, pin board link in the real group. Design pass (M5) flagged items noted in agent reports.
- 2026-09-01 (end of session): M4 IN PROGRESS — deploy not yet live. State:
  - GitHub: https://github.com/StrangeNelly/overseer-bot (all commits pushed).
  - Railway: project "rare-compassion" (4798a5cf-04ed-4668-b4b3-218b80268911), env production (a7f359ce-8b61-49a1-87a7-f1f494f7ef46), single service "@groupie/server" (05768bd8-8af7-4241-ae0f-0fcc6fa194f6), domain groupieserver-production.up.railway.app. CLI installed + authed; repo linked. Railway MCP + plugin installed (MCP OAuth still pending in interactive /mcp; Supabase MCP same).
  - Variables set (via CLI): BOT_TOKEN, DATABASE_URL, SESSION_SECRET, NODE_ENV=production, PORT=3000, WEB_APP_URL=https://groupieserver-production.up.railway.app, NPM_CONFIG_PRODUCTION=false.
  - Fixed: original build failure (NODE_ENV=production stripped dev deps at install; second `npm ci` in buildCommand hit EBUSY on Railway's node_modules/.cache mount). railway.json now: buildCommand "npm run build", watchPatterns ["**"], Node pinned 22 (.node-version).
  - BLOCKER: every deployment reports SKIPPED — GitHub pushes ("No changes to watched files", stale apps/server watch paths from the original monorepo split) AND, unexplained, a CLI `railway up` deployment (a373f446-3fae-4d94-adcd-14420b6e01ba) also polled SKIPPED 6+ min. Next debug steps: dashboard → service Settings → check watch paths / root directory ("/") / any "Wait for CI"-style gate; or auth the Railway MCP and inspect; nuclear option that likely just works: delete the service, re-add from GitHub repo with root "/" (railway.json is now correct so a fresh service should build green), then re-add the 7 variables (values in local .env; NPM_CONFIG_PRODUCTION=false; regenerate domain and update WEB_APP_URL to match).
  - After deploy is green: BotFather /newapp (title Groupie, short name "board", 640x360 placeholder image already sent to owner, Web App URL = the Railway domain), then Railway var MINI_APP_URL=https://t.me/overseergroupbot/board, then pin /groupie reply in the TEST group only (soft-launch decision in docs/decisions.md).
  - Local bot/dev server STOPPED (Railway will be the only poller; Telegram drops queued updates >24h old).
  - Then: design pass + flesh-out before real-group launch (M5 flagged items in status above; agent design notes in M3 reports).
- 2026-09-02: M4 DEPLOYED. Root cause of the SKIPPED mystery: the original service kept apps/server/** watch paths from Railway's monorepo auto-split, and no commit after the failed build touched that dir — so every deploy (even CLI `railway up`) was skipped as "No changes to watched files". Fix: created fresh service "groupie" (9619a4a6-26a4-4a65-90be-3425870e04ad) via `railway add`, deployed by `railway up` (source = CLI upload, GitHub NOT auto-connected yet — connect via dashboard Settings -> Source for push-to-deploy; until then deploy with `railway up --service groupie`). Domain: https://groupie-production-3bbd.up.railway.app (WEB_APP_URL set; all 7 vars on the new service). Verified live: /health ok, SPA 200, dev-auth 404, board 401 unauth, cross-site bin 403. Bot polls from Railway — do NOT run `npm run dev` locally while it's up (409 fight); for local dev, stop/sleep the Railway service or use a second bot token. OLD service "@groupie/server" (05768bd8) is broken/superseded — pending owner ok to delete. NEXT: BotFather /newapp (Web App URL = the domain above), set MINI_APP_URL var, pin in TEST group. Ranging board feature in build (contract in packages/shared/src/api.ts, spec in docs/decisions.md round 3).
- 2026-09-02: Ranging board SHIPPED to production (Opus-built, Fable-reviewed, browser-verified; deploy green). WEB_ONLY=1 dev mode added for local work while Railway owns the bot. Ranging fills as continuous poller uptime accumulates (streaks need unbroken 5-min coverage; Railway poller started ~2026-09-01 23:30Z). STILL PENDING (owner): BotFather /newapp + MINI_APP_URL var; ok-to-delete old Railway service @groupie/server; connect GitHub repo to service groupie in dashboard for push-to-deploy (currently deploys via railway up).
- 2026-09-02: Watchlist alerts SHIPPED (commit + deploy green, migration 0003 applied): /groupie watch|unwatch|watchlist|alerts|set commands, nuke + buy-opp alerts posting into the chat as replies to the original call message, per-group clamped settings in groups.settings.alerts, atomic cooldowns. Feature research done -> docs/research-features.md (top-10 synthesis; #1/#2/#5/#9 earmarked for the design pass, #3 anti-fatigue refinements as follow-up). Service moved to Singapore (was Amsterdam) — board latency much improved. NEXT: owner picks from top-10; design pass (brief in docs/design-brief.md).
- 2026-09-02: Rug auto-removal SHIPPED and verified live: SYNDROMICS auto-binned (rug_floor) by the first production sweep; MICA died via liquidity_floor; WATCH + pokepad tracking healthy (second caller @pwnzssg attributed). Refetch debounce 6s. 124 tests. NEXT: owner's top-10 picks, design pass.
- 2026-09-02: OWNER SUPERSEDED SOFT LAUNCH — bot added to the REAL group ("hammertime", slug 8e5ae7c6, group id 2). Registered cleanly via my_chat_member; 3 calls ingested (MICA died liquidity_floor, WATCH + pokepad active), 2 callers attributed, 0 errors in logs, snapshots 240KB. Design pass is now polishing a LIVE product — prioritize accordingly.
- 2026-09-02: Rug probation v2 SHIPPED (migration 0004 applied, deploy green, 160 tests): hide 1h under $8K -> 30-min probation 24h -> revival $30K/3h into Reviving section -> expiry = permanent rug. Armed curve floor retired; sub-$9K re-mentions inert (6.5a). Live: HDFI running 2.4x ($256K->$609K), pokepad approaching hide threshold. NEXT: design pass go-ahead + owner top-10 picks still open.
- 2026-09-02: Session handoff SHIPPED (migration 0005, 184 tests, smoke-tested mint/redeem/replay): Mini App 'Full board' button opens the browser already signed in via one-time 60s hashed token, atomic single-winner redemption on the DB clock. Login-CSRF shape of GET redemption noted and accepted (60s TTL, one-shot, no data exposure). Design brief v2 + prompt delivered; owner running Claude Design app with local codebase attached. NEXT: owner's chosen design direction -> implementation milestone.
- 2026-09-02: DEGEN NEON DESIGN SHIPPED to production. overseer identity live across half-sheet/mobile/desktop; motion system + cached paint in; 184 tests; browser-verified vs canvas at 375/1440 with live data before deploy. Flagged API gaps for follow-up: died-rail hidden-probation count, board todayCallCount, mcap-at-death field, ranging summary prefetch. Owner still to do: BotFather /setuserpic (overseer-profile-512.png) + /myapps cover swap (overseer-cover-640x360.png); Railway Hobby upgrade. Product display name is now overseer.
- 2026-09-02: SLEEPERS SHIPPED and live-verified: first production scan populated 20 entries across all 4 bands minutes after deploy (GT 10-page volume sweep, tapering vol curve, turnover rank, per-group exclusion, X-only default). App-wide X pills live. 215 tests. Still pending owner: BotFather profile/cover swap, Railway Hobby upgrade. Repo remains the source of truth for all decisions (rounds 1-9).
- 2026-09-02 (evening): BIG BUNDLE SHIPPED — browser Telegram login LIVE (OIDC armed, /api/auth/telegram/available true; BotFather OIDC client configured, redirect URI exact-matched), liquidity-death persistence + newborn grace (rounds 11), mcap-only revival $30K (round 13, zombie flap sealed), collapse-rug + Retraced honesty (round 10), local-dev bot guardrail (RUN_BOT), inert window chips on RNG/SLPRS. 298 tests. Railway Hobby upgraded by owner. Outage root cause documented (local dev server 409 war — guardrail ends the class). Remaining owner-visible roadmap: alert anti-fatigue, watchlist presence, $100-at-call, recap, caller cards, API gaps batch, X launch monitor.
- 2026-09-02: SLEEPERS v2 + RANGING SHORT HOLDS SHIPPED (migration 0007 applied, deploy green, 342 tests): time-in-band durations 3h-1m from GT OHLCV residency, $1M-3M band at 2w/1m only, LP/mcap >=2% floor (FORESKIN case), Ranging 30m/1h chips gated to bands hi<=$500K. Resolved round-9-vs-14 conflict: scan pool-age ceiling now inBandMaxDays (35d), round 9's 10-day ceiling re-applied at serve time for short-duration views (passesServeAgeCeiling). OHLCV paced 4s so scans never starve nuke alerts. Boot scan drew GT 429s and aborted (restart floods budget with catch-up polls) -> committed 5-min boot hold-off + 10-min failure retry, NOT yet deployed (working tree shared with in-flight round-15 quick-wins agent; ships with that batch; prod self-heals at the next 3h scan). Railway warns railway.json config-as-code is deprecated, works until 2026-12-01 - migrate to .railway/railway.ts sometime before then.
- 2026-09-02: ROUND 15 QUICK WINS SHIPPED (migration 0008 applied, deploy green, 391 server tests, browser-verified 1440/390 with live data): watch button on cards (web+bot share watchlist.ts; cap 3/member via added_by behind pg_advisory_xact_lock; WATCHING-YOU marks the reader own slots), API honesty batch (mcap-at-death at all death sites, hiddenProbationCount, todayCallCount off the member local midnight via ?tz=, desktop ranging summary), dead tokens re-checked every 3h for 48h then daily (OMNI-class wrong-deaths self-heal fast now), LinkPills on spotlight/hero cards + WEBSITE pill app-wide. 17-agent adversarial review workflow confirmed 10 findings, all fixed pre-deploy: int4 path-id 404s, group-scoped watch lookup (killed a cross-group id oracle), groupId on watch_changed/call_binned SSE events, bot cap refusal no longer leaks orphan tokens rows, permanent rugs auto-release watch slots, died-rail unwatch pill on desktop, collapse-death WHERE guard, honest mcap-at-death staleness comments, pending-state clobber fix. Boot sleeper-scan hold-off verified working on this deploy (no 429). NEXT: design pass 2 (owner runs docs/design-prompt-2.md in Claude Design after restarting the app on Fable 5.1), then X launch monitor decision.
- 2026-09-02 (late): ROUND 16 SHIPPED — design pass 2 + watch everywhere (deploy green 23:14Z, 447 server tests, browser-verified 1440/390 with live data, both watch paths round-tripped net-zero on the real group). Desktop is now the 3A opportunity-first composition (FRESH rail / IN PLAY zones RUNNERS-RETRACED-REVIVING-ON WATCH with client rank rules, gauge, hero sparks / right rail DIED-RANGING-SLEEPERS summaries), Ranging + Sleepers view headers and band bars, mobile zone chips + tone bands, 3G watch-move ceremony (throttled, hysteresis, FIFO). Watch everywhere: POST /api/g/:slug/watch {address} + DELETE /watch/:address (bot parity), BoardResponse.watchlist (whole active watchlist incl. chat/Sleepers watches) drives ON WATCH on desktop AND a mobile tab, SleeperEntry.watched/watchedByMe, WATCH pill on every coin surface; SSE relevance = call OR active watch; orphan tokens (no call, no watch) never polled. Built by a 2-agent Opus workflow, reviewed by a 56-agent adversarial workflow (45 confirmed findings), fixed by a second 2-agent workflow + per-dimension verify, 3 integrator residuals fixed inline. GT saga same day: shared-IP 429s -> adaptive budgeter (2s..15s gap) + boot hold-off + scan retry; first full Sleepers v2 scan landed 22:27Z (40 kept). NEXT: owner runs docs/design-prompt-onepager.md in the design session (chat announcement graphic); then round 16b GT diet (batch curve polls via /pools/multi, incremental residency, poll priority) as the next backend milestone; X launch monitor decision still open. Owner chores unchanged: BotFather image swaps, GitHub->Railway connect.
- 2026-09-03: ROUND 16b SHIPPED — GeckoTerminal budget diet (deploy green 00:03Z, 502 server tests, migrations 0009 residency_measured_at + 0010 group_members.display_name applied): curve + dead tiers batched via /pools/multi (30/call; absent pool = unknown, never $0; failed dead prefetch defers+stamps curve corpses), incremental sleeper residency (carry forward same-band entries, daily re-verify, no carry when last-hour trades = 0), budgeter policy as a pure function with poll-before-scan priority + 60s scan aging bound. Round 16c bundled: watch holder display names cached on membership checks and chat commands (ON WATCH names the holder, mention name fallback). Web fixes: FRESH-rail hover strip (starts past the coin, compact CA/WEB, never pins on click, above the numbers), Sleepers hover/click duplicate removed. Adversarial workflow: 14 findings fixed + verified, 2 integrator residuals fixed inline. Baseline before: ~9 GT back-offs/h, scans 20-30 min. Next GT consumer is RESOLUTION (up to 2 calls/token, 6/tick) — batch it next if back-offs persist. NEXT: round 17 Sleepers (no-stocks toggle via the "• Robinhood Token" issuer suffix + HOOD/leveraged list, 30m/1h holds via 15-min candles, bands $1-3M regular + $3-5M + $5-8M).
- 2026-09-03: ROUNDS 17 + 19 SHIPPED. R17 (deploy 00:49Z, migration 0011 is_stock): Sleepers no-stocks toggle (issuer suffix "• Robinhood Token" + leveraged + HOOD address; per-kind keep AND serve cuts), 30m/1h holds from 15-min candles (minute evidence authoritative under 3h, gated by last-hour trades, capped under 3h), seven bands ($1-3M regular, $3-5M, $5-8M), xOnly/excludeStocks echoed; owner UX fixes (rows read called->now, FRESH-rail hover strip past the coin / never pins / above numbers, Sleepers no double reveal, mobile chips in the ctl panel). First full R17 scan: 55 kept, 14 stocks flagged, 13 in band 24h+. R19 (deploy 01:40Z, migration 0012 mcap_at_watch + buy_opp_armed): BUY OPP measures from the watch-time mcap (fresh cache or first snapshot after added_at), fires once on an explicit armed flag, re-arms on recovery; peak-window settings retired; ON WATCH rows print "watched at $X · -N% since". Research stream -> docs/research-features-2.md (owner reframed to DISCOVERY-first). Decisions 17b (wrong-chain death + unresolved back-off + batched resolution), 18 (Uniswap launch alerts — ON-CHAIN via Alchemy; GT new_pools cannot enumerate 20K launches/day), 20 (graduation alerts filtered by X+website and launch-block bundle share). GT reality after 16b: budgeter pinned at the 15s ceiling in steady state; scans complete but take ~30 min; the dud 0x020e (Base contract) still costs 2 resolution calls/45s -> 17b is the lever. Owner action pending: Alchemy key (ALCHEMY_API_KEY) for rounds 18/20.
- 2026-09-03: ROUND 17b SHIPPED (deploy 02:27Z, no migration; 642 server + 20 shared + 10 web tests — apps/web now has a vitest suite): wrong-chain detection (both Robinhood sources miss AND the CA is >=60 min old -> DexScreener any-chain check on every failed attempt; pairs elsewhere -> death 'wrong_chain:<chain>' guarded on phase=unresolved, watch slots released, WRONG CHAIN · BASE on the board, corpse stays on the daily cadence with zero market calls), unresolved back-off (45s/15m -> 5min/6h -> hourly/48h; "not indexed yet · Nh"), batched resolution (/tokens/multi + /pools/multi per tick, staged failures, omitted/failed batches stamped). Expect the Base dud 0x020e to die wrong_chain:base on its next hourly attempt. NEXT: discovery build (rounds 18 + 20: shared Alchemy client, Uniswap launches + filtered PONS graduations, GRADUATED/LAUNCHES zones, capped chat alerts) — builds against mocks; dormant in prod until the owner sets ALCHEMY_API_KEY.
- 2026-09-03: DISCOVERY SHIPPED (rounds 18 + 20; commit 87eadc6, deploy 05:17Z, migration 0013 applied; 834 server + 20 shared + 40 web tests). DORMANT in prod until the owner sets ALCHEMY_API_KEY (boot log: "chain listener disabled"; /discovery answers enabled:false; bot says "Discovery: off (not configured)"). What turns on with the key: one eth_getLogs per 20s range over the Uniswap v2 factory / v4 PoolManager / PONS factory + graduation hook (3-block head lag, CU-weighted meter logged hourly, chain_cursor heartbeat served as lastTickAt); launches = first pool for a new coin with a WETH/USDG/native quote, opening figure = the DEPOSIT (v2 Mint sum; v4 receipt Transfers net of same-tx Swaps; native tx.value, an upper bound), never buys; bundle facts = pool OUTFLOWS only (hook/burn/curve excluded), graduations measured from the original TokenLaunched block on the curve (curve-as-sink verified live 2026-09-03, docs/research-onchain.md); enrichment/lock reads (rotated by lock_attempted_at)/re-enrichment/delivery on their own 30s loop; alerts = per-group one-shot decisions (discovery_alert_decisions) under an advisory lock + partial unique index, 3/h cap, 15-min freshness; /overseer set launch <eth> (0 mutes) and set grads on|off. Web: DISCOVERY view + rail card + DSCVR chip, three independent filter chips (xweb/bundles/stocks), stall verdict read against the server's Date header, 120s poll while mounted. Round pipeline: 2-agent build -> 5-dimension review (usage limit killed 14 refuters) -> fix round with 16 per-item verifiers -> residual round with 4 lenses -> 7 integrator fixes inline. Browser pass NOT done this round (browser tools blocked by the permission classifier) — verified at HTTP level + 40 web render/unit tests; owner should eyeball the live DISCOVERY tab/rail card once. 17b verified live: the Base dud died wrong_chain:base at 03:27Z; GT back-offs ~4/h (all inside the sleeper scan). NEXT: owner sets the Alchemy key (then watch "chain client: N CU/hour" on day one); GitHub->Railway connect; BotFather image swaps; railway.json -> .railway/railway.ts before 2026-12-01.
- 2026-09-03: ALCHEMY KEY SET + TWO DEPLOYS. Owner created the Alchemy app (Node API only) and set ALCHEMY_API_KEY; first live ticks (10:47Z) exposed two plan limits — free tier = 10-block eth_getLogs range on this chain AND a compute-units-per-second cap (429s on 40 sequential chunks) -> free tier is NOT viable for a 20s listener; owner upgraded to PAYG. viem printed the request URL (with the key) into error messages -> all chain errors now logged via summarizeRpcError (status/code/provider text only); owner rotated the key. Deploy fdd47ea (11:26Z): listener hardening (learned range cap + chunking within a 40-chunk budget, tick sizing from the learned cap with a mid-tick re-plan, 250ms chunk pacing, 429 back-off 60s->10min, graduation launch hunt by the PONS pool's creation time via block-timestamp bisection, no unbounded scans), peak $30M · 2.3x (· back under call) on every call card/row/died card/watch row (the 'mark-to-market since call' critique), Sleepers/Discovery hover strip in the cyan accent. Deploy 8fe60d2 (12:26Z, migration 0014 applied): ROUND 21 — member verdict (MARK DEAD pill with SURE? guard on every live surface incl. half-sheet/Ranging/unresolved; RESTORE on member deaths — in the hover strip on desktop rows; /overseer dead|undead; exempt from auto-revival; RESTORE 409 when the coin itself died), flatline deaths (>=85% off peak, <$500 & <=5 trades/24h, six hours of COVERED readings: 6 readings, gap <=125 min — sized to the hourly idle tier; revival needs $2K volume; curve corpses read on GT; flatline gate before the graduation exception), Sleepers drop-reason telemetry (per-reason summary + per-coin 'sleeper drop:' / 'sleeper hidden:' lines, unknown readings separate from floor failures — built to answer why $CUM (first listed 08:32Z, after its pump) was not kept during its 23:30Z-05:00Z flat stretch), discovery ticks pause on 401/403 too. Tests: 28 shared / 948 server / 121 web. Pipeline: Opus builds AND Opus verifies from now on (57 Fable verifiers overnight ate 77% of the weekly Fable allowance). Browser pass still blocked by the permission classifier — HTTP-level + render tests only; owner to eyeball. LIVE STATE at 12:30Z: the service holds the REVOKED key (owner's re-entry did not save; 401 back-off pausing ticks 60s->10min); once the new key is committed in Railway and deployed, the PAYG listener should tick clean. NEXT: read the first 'sleeper scan: in-band dropped' lines (~13:50Z scan) and decide the quiet-coin gate; watch 'chain client: N CU/hour' on PAYG day one; RH|BASE chain switch on Sleepers (round 22 candidate); owner chores unchanged (BotFather images, GitHub->Railway connect, railway.json -> .railway/railway.ts before 2026-12-01).
